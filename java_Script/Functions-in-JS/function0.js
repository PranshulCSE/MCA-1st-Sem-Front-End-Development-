// Lets Understand the Concept of Functions in JavaScript

function Greet(name){
    const message=console.log(`Hello Mr./Ms. ${name}`);
}
    Greet("ARMAAN");

    //  function is the Keyword
    //  Greet is the name of Function
    // name is the Argument
    
    // Greet("ARMaAN") Here we are calling the Function
    //  ARMAAN is the Parameter we passed to the Function



    // Functions Returning Some Value

 function sum( a,b ){
    return a+b;
    // Here we are Returning Some Value
 }  
 
 console.log(sum(5,9));
