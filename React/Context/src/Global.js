import { createContext } from "react";

const GlobalData = createContext("Pranshul Threja");

export default GlobalData;


// Use Context is nothing but creating a file globally which can be accessible by any component